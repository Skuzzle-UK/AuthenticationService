namespace AuthenticationService.Constants;

/// <summary>
/// Well-known username values used by the seeder. Anything that pins a *specific* username
/// in code rather than configuration goes here.
/// </summary>
public class UserConstants
{
    /// <summary>
    /// The default administrator's username, set at seed time.
    /// </summary>
    public const string Admin = "admin";
}